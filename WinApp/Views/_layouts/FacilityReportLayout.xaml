<UserControl x:Class="WinApp.Views.FacilityReportLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinApp.Views"
             xmlns:vst="clr-namespace:Vst.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <local:PageHeader x:Name="Header" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Body" Orientation="Vertical">
                <!-- Phần 1: Thông tin cơ sở -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" 
                        Background="#F5F5F5" Margin="10" Padding="15">
                    <StackPanel>
                        <TextBlock Text="THÔNG TIN CƠ SỞ" 
                                   FontSize="16" FontWeight="Bold" 
                                   Foreground="#333333" 
                                   Margin="0,0,0,10"/>

                        <!-- Thông tin cơ sở -->
                        <StackPanel x:Name="FacilityInfo" Orientation="Vertical">
                            <!-- Các thông tin cơ sở sẽ được thêm động ở đây -->
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Phần 2: Các bảng dữ liệu sẽ được thêm động -->
                <StackPanel x:Name="TablesContainer" Orientation="Vertical" Margin="10,0,10,10">
                    <!-- Các bảng với filter sẽ được thêm vào đây -->
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Border x:Name="Footer" Grid.Row="2" Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center">
                    <TextBlock Text="Tổng số: " FontWeight="SemiBold"/>
                    <TextBlock x:Name="Total" />
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>